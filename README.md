# Data-typescript

# Simple exemple
this simple data make with typescript you can use like make web app with `react` , exemple for user data:

```ts
 {
        id: 1,
        name: "adam",
        age: 15,
    },
    {
        id: 2,
        name: "jad",
        age: 10,

    }
```
```ts
export interface data{
    id: number;
    name: string;
    age:number;

}
```
and this is data type.

# Find file

find this file at `src` file you find file name `datatype.ts` and `data.ts`

And don't forgot to strat or frok


